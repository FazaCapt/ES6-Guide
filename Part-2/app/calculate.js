// ===== Exprting Expression and by default =====

const add = (x, y) => {
    return x + y;
}
// === Cara 1 || Gak bisa, akan error di const
// export default const multiply = (x, y) => {
//     return x * y;
// }

// === Cara 2
// const multiply = (x, y) => {
//     return x * y;
// }

// export {add, multiply};
// export default multiply;